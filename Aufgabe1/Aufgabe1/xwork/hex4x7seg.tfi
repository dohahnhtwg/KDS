


// Instantiate the module
hex4x7seg instance_name (
    .rst(rst), 
    .clk(clk), 
    .en(en), 
    .swrst(swrst), 
    .data(data), 
    .dpin(dpin), 
    .an(an), 
    .dp(dp), 
    .seg(seg)
    );


